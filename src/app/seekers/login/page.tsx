import { LoginForm } from "./login-form";

export const metadata = {
  title: "log in | attn:seeker",
  description: "welcome back, seeker.",
};

export default function SeekersLoginPage() {
  return <LoginForm />;
}
