import ProfilePageClient from "./profile-client";

export const metadata = {
  title: "profile | attn:seeker",
  description: "manage your seekers profile.",
};

export default function ProfilePage() {
  return <ProfilePageClient />;
}
