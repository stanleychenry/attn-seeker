import { SignupForm } from "./signup-form";

export const metadata = {
  title: "sign up | attn:seeker",
  description: "become a seeker. it's free.",
};

export default function SeekersSignupPage() {
  return <SignupForm />;
}
