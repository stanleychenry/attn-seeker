import DashboardClient from "./dashboard-client";

export const metadata = {
  title: "dashboard | attn:seeker",
  description: "your seekers dashboard.",
};

export default function SeekersDashboardPage() {
  return <DashboardClient />;
}
