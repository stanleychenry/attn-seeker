import StoreClient from "./store-client";

export const metadata = {
  title: "rewards store | attn:seeker",
  description: "spend your yap dollars on exclusive rewards.",
};

export default function SeekersStorePage() {
  return <StoreClient />;
}
