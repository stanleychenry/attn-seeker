export const metadata = {
  title: "contact | attn:seeker",
  description:
    "get in touch with the attention seeker. agency enquiries, partnerships, and general questions.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
