export const metadata = {
  title: "case studies | attn:seeker",
  description:
    "real results for real brands. see how we grow challenger brands through organic social media.",
};

export default function WorkLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
